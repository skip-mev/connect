# my global config
global:
  scrape_interval: 100ms  # Adjust this as necessary
  evaluation_interval: 1s
scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["oracle:8002", "blockchain:26660"]